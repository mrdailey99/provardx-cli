# summary

Start the ProvarDX MCP server.

# description

Starts a long-running MCP (Model Context Protocol) server over stdio that IDE agents can connect to. The server exposes ProvarDX capabilities as MCP tools that can be used by AI assistants and other MCP clients.

# examples

- Start the ProvarDX MCP server

  <%= config.bin %> <%= command.id %>
